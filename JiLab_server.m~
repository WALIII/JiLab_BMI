
% Set up server side:

t = tcpip('0.0.0.0', 30000, 'NetworkRole', 'server');


fopen% this will wait until a connection is recieveed 

while i == 1;
    if t.BytesAvailable >1;
    data = fread(t, t.BytesAvailable);
    disp('data is available!');
    i = 2;
    end
end

% syntax: XXXYYY = XXX is degrees, YYY is time in seconds


a = 123002;
a=num2str(a);
disp([a(1:3), '  degrees for ', a(4:6),' seconds